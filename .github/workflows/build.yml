name: Build and release PDF

on:
  workflow_dispatch:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: write

concurrency:
  group: pdf-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    container: texlive/texlive:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Trust the workspace
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global --add safe.directory "$GITHUB_WORKSPACE/OpenLogic"

      - name: Build the Chinese edition with LuaLaTeX
        run: make slc-screen.pdf slc-print.pdf slc-print-cover.pdf

      - name: Report build failure
        if: failure()
        run: |
          {
            echo '```text'
            tail -n 120 slc-screen.log 2>/dev/null || true
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Package PDFs
        run: |
          mkdir -p dist
          cp slc-screen.pdf dist/sets-logic-computation-zh-screen.pdf
          cp slc-print.pdf dist/sets-logic-computation-zh-print.pdf
          cp slc-print-cover.pdf dist/sets-logic-computation-zh-print-cover.pdf

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: sets-logic-computation-zh
          path: dist/*.pdf

      - name: Release
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: 集合、逻辑与计算 Build ${{ github.run_number }}
          files: dist/*.pdf
          generate_release_notes: true
